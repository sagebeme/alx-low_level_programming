#include "main.h"


/**
	* _islower - checks if integer is a lowercase letter
	* @c: letter
	* Description: checks if integer is a lowercase letter
	* Return: boolean
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
