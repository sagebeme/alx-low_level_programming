#include "main.h"

/**
	* _abs - returns absolute value of an integer
	* @n: number
	* Description: return absolute value of an integer
	* Return: int
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
